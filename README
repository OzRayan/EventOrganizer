# This app was coded in IntelliJ 2020.3.3 pro version.
# The app is connecting to phpMyAdmin server, without database the can not run.

# In order to run the app:
    * For this project it was used the XAMPP v3.2.4, to connect to a server,
    * Apache and MySQL modules should be active.
    * JDBC DRIVER: - "com.mysql.jdbc.Driver"
    * URL to server: - "jdbc:mysql://localhost/eems" -> eems, the table name
    * user: - "root"
    * password: - ""

# This code was written for study purpose only!